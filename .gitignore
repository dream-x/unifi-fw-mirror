# real config — carries your device list, proxy and paths
config.json

# proxy credentials
*.env
!.env.example

# mirrored artifacts, if anyone points dest at the checkout
*.bin
*.deb
*.part
manifest.json

__pycache__/
*.py[cod]
.venv/
.DS_Store
